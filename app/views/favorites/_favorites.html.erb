<%if book.favorited_by?(current_user)%>
 <p><%=link_to book_favorites_path(book.id), method: :delete ,remote: true do%>♥<%=book.favorites.count%><%end%></p>
<%else%>
<P><%=link_to book_favorites_path(book.id),method: :post ,remote: true do%>♡<%=book.favorites.count%><%end%></P>
<%end%>